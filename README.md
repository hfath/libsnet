# libsnet

libsnet is a simple, secure, TCP communications library with support for line & block IO, SSL, SASL, ZLIB.

This is a fork from <https://sourceforge.net/projects/libsnet/>, there isn't an official presence on github, yet.
